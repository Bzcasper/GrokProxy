# GrokProxy Environment Configuration Example
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=8000
HOST=0.0.0.0

# ==================== Database Configuration ====================
# PostgreSQL/Neon connection string
# Example: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@host.neon.tech:5432/grokproxy?sslmode=require

# Database connection pool settings
DB_POOL_MIN_SIZE=10
DB_POOL_MAX_SIZE=20

# ==================== API Authentication ====================
# Password(s) used to authenticate API requests
# Can be a single password or comma-separated list
API_PASSWORD=your_secure_password_here

# ==================== Observability ====================
# Logging configuration
LOG_LEVEL=info

# Sentry error tracking (optional - leave empty to disable)
SENTRY_DSN=

# Sentry environment (development, staging, production)
SENTRY_ENVIRONMENT=production

# Prometheus metrics (true/false)
METRICS_ENABLED=true

# ==================== Session Management ====================
# Number of requests before rotating a session
SESSION_ROTATION_THRESHOLD=500

# Maximum age of a session in hours
SESSION_MAX_AGE_HOURS=24

# Failure rate threshold (0.0-1.0) before quarantining session
SESSION_FAILURE_THRESHOLD=0.2

# Health check interval in seconds
SESSION_HEALTH_CHECK_INTERVAL=30

# ==================== Rate Limiting ====================
# Per-user rate limit (requests per minute)
USER_RATE_LIMIT_PER_MINUTE=60

# Per-session rate limit (requests per second)
SESSION_RATE_LIMIT_PER_SECOND=10

# ==================== Ngrok Configuration ====================
# Ngrok authentication token for external HTTPS access
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# Python Configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Note: Cookies and API keys are configured in cookies.yaml
